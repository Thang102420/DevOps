version: '3.8'

services:
  vietthang-store:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vietthang_store_app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
